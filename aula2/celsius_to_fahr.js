// Pergunta o valor da temperatura em celsius a ser transformada
var celsius = parseFloat(prompt("Digite a temperatura em graus Celsius: "));

// Calcula a conversão
var fahr = (celsius * 1.8) + 32;

// Imprime o resultado final
console.log("A temperatura em Fahrenheit é: " + fahr);
